extends layout

block content
  - views = views.concat(["/test-calendars     ", "/test-organizations     "])
  .page-header.text-center
    each page, index in views
      - page = page.slice(0,-5)
      if index !== 0
        span &nbsp; - &nbsp;
      a(href=page)=page